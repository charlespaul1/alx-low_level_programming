#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints single digits of base 10 fro 0
 *
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 48; n <= 57; ch++)
	{
		putchar(ch);
	}
	putchar(10);
	return (0);
}
